version: '3.7'

services:
  db:
    image: postgres:10.1-alpine
    volumes:
      - postgres_data:/var/lib/postgresql/data/
  
  web:
    build: .
    volumes:
      - .:/usr/src
    ports:
      - 80:8000
      - 443:8000
    depends_on:
      - db

volumes:
  postgres_data: